name: Build and push PipeWarden container images

on:
  push:
    branches: [main]
    tags: ["v*"]
    paths:
      # PipeWarden main image (container/Dockerfile)
      - 'container/**'
      - 'scripts/**'
      - 'policy/**'
      - 'proxy/**'
      # PipeWarden proxy image (proxy/Dockerfile)
      - 'proxy/Dockerfile'
      - 'proxy/requirements.txt'
      - 'proxy/addon.py'
      - 'proxy/dns_server.py'
      - 'proxy/proxy-entrypoint.sh'
      # PipeWarden runner image (runner/Dockerfile)
      - 'runner/**'
      # Workflow itself
      - '.github/workflows/build-image.yml'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  REPO: ${{ github.repository }}

jobs:
  build-images:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write       # keyless cosign signing via OIDC
      attestations: write   # GitHub-native build provenance

    steps:
      - uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Install cosign
        uses: sigstore/cosign-installer@v3

      - name: Install Syft (SBOM generator)
        uses: anchore/sbom-action/download-syft@v0

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # ----------------------------------------------------------------
      # nfw (orchestrator) image
      # ----------------------------------------------------------------
      - name: Docker meta for PipeWarden main image
        id: meta-nfw
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.REPO }}/nfw
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push PipeWarden main image
        id: build-nfw
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./container/Dockerfile
          push: true
          tags: ${{ steps.meta-nfw.outputs.tags }}
          labels: ${{ steps.meta-nfw.outputs.labels }}
          provenance: mode=max
          sbom: true

      - name: Attest build provenance — nfw
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.REPO }}/nfw
          subject-digest: ${{ steps.build-nfw.outputs.digest }}
          push-to-registry: true

      - name: Generate SBOM — nfw
        run: syft "${{ env.REGISTRY }}/${{ env.REPO }}/nfw@${{ steps.build-nfw.outputs.digest }}" -o spdx-json=sbom-nfw.spdx.json

      - name: Attest SBOM — nfw
        uses: actions/attest-sbom@v2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.REPO }}/nfw
          subject-digest: ${{ steps.build-nfw.outputs.digest }}
          sbom-path: sbom-nfw.spdx.json
          push-to-registry: true

      - name: Cosign sign — nfw
        run: cosign sign --yes "${{ env.REGISTRY }}/${{ env.REPO }}/nfw@${{ steps.build-nfw.outputs.digest }}"

      # ----------------------------------------------------------------
      # nfw-proxy image
      # ----------------------------------------------------------------
      - name: Docker meta for PipeWarden proxy image
        id: meta-nfw-proxy
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.REPO }}/nfw-proxy
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push PipeWarden proxy image
        id: build-nfw-proxy
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./proxy/Dockerfile
          push: true
          tags: ${{ steps.meta-nfw-proxy.outputs.tags }}
          labels: ${{ steps.meta-nfw-proxy.outputs.labels }}
          provenance: mode=max
          sbom: true

      - name: Attest build provenance — nfw-proxy
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.REPO }}/nfw-proxy
          subject-digest: ${{ steps.build-nfw-proxy.outputs.digest }}
          push-to-registry: true

      - name: Generate SBOM — nfw-proxy
        run: syft "${{ env.REGISTRY }}/${{ env.REPO }}/nfw-proxy@${{ steps.build-nfw-proxy.outputs.digest }}" -o spdx-json=sbom-nfw-proxy.spdx.json

      - name: Attest SBOM — nfw-proxy
        uses: actions/attest-sbom@v2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.REPO }}/nfw-proxy
          subject-digest: ${{ steps.build-nfw-proxy.outputs.digest }}
          sbom-path: sbom-nfw-proxy.spdx.json
          push-to-registry: true

      - name: Cosign sign — nfw-proxy
        run: cosign sign --yes "${{ env.REGISTRY }}/${{ env.REPO }}/nfw-proxy@${{ steps.build-nfw-proxy.outputs.digest }}"

      # ----------------------------------------------------------------
      # nfw-runner image
      # ----------------------------------------------------------------
      - name: Docker meta for PipeWarden runner image
        id: meta-nfw-runner
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.REPO }}/nfw-runner
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push PipeWarden runner image
        id: build-nfw-runner
        uses: docker/build-push-action@v7
        with:
          context: ./runner
          push: true
          tags: ${{ steps.meta-nfw-runner.outputs.tags }}
          labels: ${{ steps.meta-nfw-runner.outputs.labels }}
          provenance: mode=max
          sbom: true

      - name: Attest build provenance — nfw-runner
        uses: actions/attest-build-provenance@v4
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.REPO }}/nfw-runner
          subject-digest: ${{ steps.build-nfw-runner.outputs.digest }}
          push-to-registry: true

      - name: Generate SBOM — nfw-runner
        run: syft "${{ env.REGISTRY }}/${{ env.REPO }}/nfw-runner@${{ steps.build-nfw-runner.outputs.digest }}" -o spdx-json=sbom-nfw-runner.spdx.json

      - name: Attest SBOM — nfw-runner
        uses: actions/attest-sbom@v2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.REPO }}/nfw-runner
          subject-digest: ${{ steps.build-nfw-runner.outputs.digest }}
          sbom-path: sbom-nfw-runner.spdx.json
          push-to-registry: true

      - name: Cosign sign — nfw-runner
        run: cosign sign --yes "${{ env.REGISTRY }}/${{ env.REPO }}/nfw-runner@${{ steps.build-nfw-runner.outputs.digest }}"
