{
  "name": "pipewarden-native-proxy-action",
  "version": "1.0.0",
  "private": true,
  "description": "PipeWarden native-proxy GitHub Action — setup, automatic teardown, and report artifact upload.",
  "license": "MIT",
  "scripts": {
    "build": "ncc build src/main.js -o dist/setup --minify && ncc build src/post.js -o dist/teardown --minify",
    "test": "node src/decide.test.js"
  },
  "dependencies": {
    "@actions/artifact": "^6.2.1",
    "@actions/cache": "^5.2.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.1"
  },
  "overrides": {
    "undici": "^6.28.0"
  }
}
